@model List<BookManagement.DTOs.BookDto>;

<h1>Books</h1>

<a asp-controller="Book" asp-action="Create">Create Book</a>
<hr />
<a asp-controller="Book" asp-action="GetById">Search / Edit Book By Id</a>
<hr />

@if(Model.Count==0)
{
    <p>No Books Available....</p>
}
else
{
    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Author</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var book in Model)
            {
                <tr>
                    <td>@book.Id</td>
                    <td>@book.Title</td>
                    <td>@book.Author</td>
                    <td>@book.Price</td>
                    <td>
                        <a asp-action="Update" asp-route-id="@book.Id">Edit</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}